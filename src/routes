const express = require('express');
const router = express.Router();


router.get('/', (req, res) => {
res.json({ message: 'Hello from Automated CI Pipeline app!' });
});


router.get('/health', (req, res) => {
res.json({ status: 'UP' });
});


module.exports = router;